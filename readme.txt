Compile the program: 
	javac *.java

Run the Program:
	java TrafficJam

Change the state to find the solution for by changing line 16 in TrafficJam.java (the line at the start of the main() method)
State startState = TrafficJam.getStateA(); // for initial state 1
State startState = TrafficJam.getStateB(); // for initial state 2
State startState = TrafficJam.getStateC(); // for initial state 3